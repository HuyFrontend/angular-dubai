import { MBCHttpProvider, MBC_HTTP_PROVIDER, providerFactory } from './mbc-http.provider';

const GLOBAL_APP_PROVIDERS = [
    MBC_HTTP_PROVIDER
];

export {
    MBCHttpProvider,
    
    GLOBAL_APP_PROVIDERS
}